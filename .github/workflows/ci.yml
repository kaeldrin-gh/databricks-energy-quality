name: ci

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install
        run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check .

      - name: Format check
        run: ruff format --check .

      - name: Unit tests (no workspace needed)
        run: python -m pytest -q

  bundle:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - uses: databricks/setup-cli@main

      # Forks and PRs without the secrets stay green: validation and deploy
      # only run when a Free Edition workspace token is configured.
      - name: Check for Databricks credentials
        id: creds
        env:
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          if [ -n "$DATABRICKS_TOKEN" ]; then
            echo "configured=yes" >> "$GITHUB_OUTPUT"
          else
            echo "configured=no" >> "$GITHUB_OUTPUT"
            echo "::notice::DATABRICKS_HOST/DATABRICKS_TOKEN secrets are not set - skipping bundle validate and deploy."
          fi

      - name: Validate bundle
        if: steps.creds.outputs.configured == 'yes'
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          set -o pipefail
          # Mask the workspace user path (the workspace email would otherwise
          # show up in this public log).
          databricks bundle validate -t free 2>&1 | sed -E \
            -e 's#/Workspace/Users/[^/]+#/Workspace/Users/***#g' \
            -e 's#^( *User: ).*#\1***#' \
            -e 's#https://[a-z0-9-]+\.cloud\.databricks\.com#https://***.cloud.databricks.com#g'

      - name: Deploy bundle (main only)
        if: steps.creds.outputs.configured == 'yes' && github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          set -o pipefail
          pip install build
          databricks bundle deploy -t free 2>&1 | sed -E \
            -e 's#/Workspace/Users/[^/]+#/Workspace/Users/***#g' \
            -e 's#^( *User: ).*#\1***#' \
            -e 's#https://[a-z0-9-]+\.cloud\.databricks\.com#https://***.cloud.databricks.com#g'

      - name: Publish dashboard (main only)
        if: steps.creds.outputs.configured == 'yes' && github.ref == 'refs/heads/main' && github.event_name == 'push'
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          set -o pipefail
          python scripts/publish_dashboard.py -t free 2>&1 | sed -E \
            -e 's#/Workspace/Users/[^/]+#/Workspace/Users/***#g' \
            -e 's#https://[a-z0-9-]+\.cloud\.databricks\.com#https://***.cloud.databricks.com#g'
